<section class="dense-pack-comparison">
  <style>
    .dense-pack-comparison {
      padding: 80px 0;
      background: var(--surface);
    }
    
    .dense-pack-comparison h2 {
      text-align: center;
      font-size: 2.5rem;
      color: var(--primary);
      margin-bottom: 20px;
    }
    
    .dense-pack-comparison .lead {
      text-align: center;
      font-size: 1.2rem;
      color: var(--text-muted);
      max-width: 800px;
      margin: 0 auto 60px;
    }
    
    .density-comparison {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 40px;
      margin: 40px 0;
    }
    
    .density-card {
      background: rgba(255, 255, 255, 0.02);
      border: 2px solid rgba(0, 255, 255, 0.3);
      border-radius: 15px;
      padding: 40px;
      text-align: center;
      transition: all 0.3s ease;
      position: relative;
    }
    
    .density-card:hover {
      transform: translateY(-5px);
      border-color: var(--primary);
      box-shadow: 0 15px 40px rgba(0, 255, 255, 0.3);
    }
    
    .density-card.recommended {
      border-color: var(--primary);
      box-shadow: 0 0 30px rgba(0, 255, 255, 0.2);
    }
    
    .density-card.recommended::before {
      content: 'RECOMMENDED';
      position: absolute;
      top: -15px;
      right: 20px;
      background: var(--primary);
      color: white;
      padding: 5px 15px;
      border-radius: 20px;
      font-weight: bold;
      font-size: 0.8rem;
    }
    
    .density-meter {
      width: 120px;
      height: 120px;
      margin: 20px auto;
      border-radius: 50%;
      background: conic-gradient(
        from 0deg,
        var(--primary) 0deg,
        var(--primary) var(--density),
        var(--surface-dark) var(--density),
        var(--surface-dark) 360deg
      );
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      box-shadow: 0 0 30px rgba(0, 255, 255, 0.3);
    }
    
    .density-meter::before {
      content: '';
      position: absolute;
      width: 90px;
      height: 90px;
      background: var(--surface);
      border-radius: 50%;
    }
    
    .density-value {
      position: relative;
      z-index: 1;
      font-size: 2rem;
      font-weight: bold;
      color: var(--primary);
    }
    
    .density-card h3 {
      color: var(--primary);
      font-size: 1.5rem;
      margin-bottom: 20px;
    }
    
    .density-card p {
      font-size: 1.1rem;
      margin-bottom: 20px;
    }
    
    .density-card ul {
      list-style: none;
      padding: 0;
      text-align: left;
    }
    
    .density-card li {
      padding: 8px 0;
      display: flex;
      align-items: center;
      gap: 10px;
    }
    
    .density-card li::before {
      content: '✓';
      color: #00ff88;
      font-weight: bold;
      font-size: 1.2rem;
    }
    
    .density-card.not-recommended li::before {
      content: '✗';
      color: #ff6666;
    }
    
    @media (max-width: 768px) {
      .density-comparison {
        grid-template-columns: 1fr;
      }
    }
  </style>
  
  <div class="container">
    <h2>Why Dense Pack for Arizona Homes?</h2>
    <p class="lead">Traditional blown-in insulation can settle over time, leaving gaps that let Arizona's extreme heat infiltrate your home. Dense pack insulation is installed at high pressure to prevent settling and maximize performance.</p>
    
    <div class="density-comparison">
      <div class="density-card not-recommended">
        <h3>Standard Blown-In</h3>
        <div class="density-meter" style="--density: 90deg;">
          <span class="density-value">1.5</span>
        </div>
        <p><strong>1.5 lbs/ft³</strong></p>
        <ul>
          <li>Prone to settling</li>
          <li>Air gaps develop</li>
          <li>R-value degrades</li>
          <li>20-30% settling rate</li>
        </ul>
      </div>
      
      <div class="density-card recommended">
        <h3>Dense Pack Method</h3>
        <div class="density-meter" style="--density: 315deg;">
          <span class="density-value">3.5</span>
        </div>
        <p><strong>3.5 lbs/ft³</strong></p>
        <ul>
          <li>No settling issues</li>
          <li>Complete cavity fill</li>
          <li>Maintains R-value</li>
          <li>0% settling rate</li>
        </ul>
      </div>
    </div>
  </div>
</section>