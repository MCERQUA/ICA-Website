{# Credentials Banner Component #}
{# Usage: {% include "sections/credentials-banner.njk" %} #}
{# Variables: position (top/bottom), theme (dark/light), showPhone #}

{% set position = position or "bottom" %}
{% set theme = theme or "dark" %}
{% set showPhone = showPhone if showPhone is defined else true %}

<div class="credentials-banner {{ 'credentials-banner--' + position }} {{ 'credentials-banner--' + theme }}">
  <div class="container">
    <div class="credentials-banner-content">
      <div class="credentials-banner-items">
        <div class="credential-item">
          <img src="/images-optimized/Spray_Foam_Insurance_logo-150w.webp" 
               srcset="/images-optimized/Spray_Foam_Insurance_logo-100w.webp 100w,
                       /images-optimized/Spray_Foam_Insurance_logo-150w.webp 150w,
                       /images-optimized/Spray_Foam_Insurance_logo-200w.webp 200w"
               sizes="(max-width: 768px) 100px, 150px"
               alt="Spray Foam Insurance" 
               class="credential-logo"
               width="150"
               height="35"
               loading="lazy">
        </div>
        
        <div class="credential-item">
          <span class="credential-icon">🛡️</span>
          <span class="credential-text">Licensed <strong>ROC #326891</strong></span>
        </div>
        
        <div class="credential-item">
          <span class="credential-icon">🔒</span>
          <span class="credential-text">Bonded & Insured</span>
        </div>
        
        <div class="credential-item">
          <span class="credential-icon">⭐</span>
          <a href="https://www.bbb.org/us/az/glendale/profile/insulation-contractors/insulation-contractors-of-arizona-1126-1000090032" target="_blank" rel="noopener noreferrer" class="credential-link">
            BBB A+ Rating
          </a>
        </div>

        {% if showPhone %}
        <div class="credential-item credential-item--phone">
          <span class="credential-icon">📞</span>
          <a href="tel:{{ site.phone }}" class="credential-phone">{{ site.phone }}</a>
        </div>
        {% endif %}
      </div>
      
      <div class="credentials-banner-actions">
        <a href="https://roc.force.com/AZRoc/s/contractor-search?licenseId=a0o8y0000005GtuAAE" target="_blank" rel="noopener noreferrer" class="verify-btn">
          Verify License
        </a>
      </div>
    </div>
  </div>
</div>

<style>
  /* Credentials Banner */
  .credentials-banner {
    position: relative;
    padding: 1rem 0;
    z-index: 10;
  }

  .credentials-banner--top {
    border-bottom: 1px solid rgba(0, 255, 255, 0.2);
  }

  .credentials-banner--bottom {
    border-top: 1px solid rgba(0, 255, 255, 0.2);
  }

  .credentials-banner--dark {
    background: rgba(10, 26, 26, 0.95);
    color: var(--gray-100);
  }

  .credentials-banner--light {
    background: rgba(248, 249, 250, 0.95);
    color: var(--primary-dark);
  }

  .credentials-banner--light.credentials-banner--top {
    border-bottom-color: rgba(30, 60, 114, 0.2);
  }

  .credentials-banner--light.credentials-banner--bottom {
    border-top-color: rgba(30, 60, 114, 0.2);
  }

  /* Animated background */
  .credentials-banner::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 200%;
    height: 100%;
    background: linear-gradient(90deg, 
      transparent 0%, 
      rgba(0, 255, 255, 0.05) 45%, 
      rgba(0, 255, 255, 0.1) 50%, 
      rgba(0, 255, 255, 0.05) 55%, 
      transparent 100%);
    animation: banner-sweep 15s linear infinite;
    pointer-events: none;
  }

  @keyframes banner-sweep {
    0% {
      left: -100%;
    }
    100% {
      left: 100%;
    }
  }

  .credentials-banner-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 2rem;
  }

  .credentials-banner-items {
    display: flex;
    align-items: center;
    gap: 2rem;
    flex-wrap: wrap;
    flex: 1;
  }

  .credential-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.9rem;
    white-space: nowrap;
  }

  .credential-logo {
    height: 35px;
    width: auto;
    filter: brightness(1.1) drop-shadow(0 0 5px rgba(0, 255, 255, 0.3));
    transition: filter var(--transition-fast);
  }

  .credential-logo:hover {
    filter: brightness(1.2) drop-shadow(0 0 10px rgba(0, 255, 255, 0.5));
  }

  .credential-icon {
    font-size: 1.1rem;
    filter: drop-shadow(0 0 5px currentColor);
  }

  .credential-text {
    opacity: 0.9;
  }

  .credential-text strong {
    color: var(--accent-primary);
    font-weight: 600;
  }

  .credential-link {
    color: var(--accent-primary);
    text-decoration: none;
    transition: all var(--transition-fast);
    position: relative;
  }

  .credential-link::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 0;
    height: 1px;
    background: var(--accent-primary);
    transition: width var(--transition-fast);
  }

  .credential-link:hover::after {
    width: 100%;
  }

  .credential-link:hover {
    color: var(--accent-secondary);
    text-shadow: 0 0 5px currentColor;
  }

  .credential-phone {
    color: var(--accent-primary);
    text-decoration: none;
    font-weight: 600;
    font-size: 1rem;
    transition: all var(--transition-fast);
  }

  .credential-phone:hover {
    color: var(--accent-secondary);
    text-shadow: 0 0 5px currentColor;
  }

  .credential-item--phone {
    margin-left: auto;
  }

  /* Verify Button */
  .verify-btn {
    display: inline-flex;
    align-items: center;
    padding: 0.5rem 1.25rem;
    background: rgba(0, 255, 255, 0.1);
    color: var(--accent-primary);
    text-decoration: none;
    border: 1px solid var(--accent-primary);
    border-radius: 25px;
    font-size: 0.875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    transition: all var(--transition-fast);
    white-space: nowrap;
  }

  .verify-btn:hover {
    background: var(--accent-primary);
    color: var(--primary-dark);
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(0, 255, 255, 0.3);
  }

  .verify-btn:active {
    transform: translateY(0);
  }

  /* Light theme adjustments */
  .credentials-banner--light .credential-text strong {
    color: var(--primary);
  }

  .credentials-banner--light .credential-link,
  .credentials-banner--light .credential-phone {
    color: var(--primary);
  }

  .credentials-banner--light .credential-link:hover,
  .credentials-banner--light .credential-phone:hover {
    color: var(--primary-dark);
  }

  .credentials-banner--light .verify-btn {
    background: rgba(30, 60, 114, 0.1);
    color: var(--primary);
    border-color: var(--primary);
  }

  .credentials-banner--light .verify-btn:hover {
    background: var(--primary);
    color: white;
    box-shadow: 0 4px 15px rgba(30, 60, 114, 0.3);
  }

  /* Mobile Responsive */
  @media (max-width: 968px) {
    .credentials-banner-content {
      flex-direction: column;
      gap: 1.5rem;
    }

    .credentials-banner-items {
      justify-content: center;
    }

    .credential-item--phone {
      margin-left: 0;
    }

    .credentials-banner-actions {
      display: flex;
      justify-content: center;
    }
  }

  @media (max-width: 640px) {
    .credentials-banner {
      padding: 1.5rem 0;
    }

    .credentials-banner-items {
      gap: 1rem;
      justify-content: space-around;
    }

    .credential-item {
      font-size: 0.85rem;
    }

    .credential-logo {
      height: 30px;
    }

    .credential-item--phone {
      width: 100%;
      justify-content: center;
      margin-top: 0.5rem;
    }

    .verify-btn {
      padding: 0.625rem 1.5rem;
    }
  }

  @media (max-width: 480px) {
    .credential-text {
      display: none;
    }

    .credential-item {
      gap: 0.25rem;
    }

    .credential-item strong {
      display: inline;
    }
  }
</style>