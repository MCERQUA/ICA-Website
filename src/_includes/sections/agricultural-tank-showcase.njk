<!-- Agricultural Tank Insulation Showcase Section -->
<section class="agricultural-tank-showcase">
    <div class="container">
        <div class="section-header">
            <h2 data-text="Agricultural Tank Insulation Expertise">Agricultural Tank Insulation Expertise</h2>
            <p class="section-subtitle">Protecting Arizona's Water Resources with Advanced Insulation Solutions</p>
        </div>
        
        <div class="tank-showcase-grid">
            <!-- Water Conservation Stats -->
            <div class="showcase-card stats-card">
                <div class="card-inner">
                    <h3>Water Conservation Impact</h3>
                    <div class="stats-grid">
                        <div class="stat-item">
                            <div class="stat-number" data-value="80">80%</div>
                            <div class="stat-label">Evaporation Reduction</div>
                        </div>
                        <div class="stat-item">
                            <div class="stat-number" data-value="500">500+</div>
                            <div class="stat-label">Gallons Saved Daily</div>
                        </div>
                        <div class="stat-item">
                            <div class="stat-number" data-value="30">$30K</div>
                            <div class="stat-label">Annual Savings</div>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Tank Types -->
            <div class="showcase-card types-card">
                <div class="card-inner">
                    <h3>Tank Types We Insulate</h3>
                    <div class="tank-types-list">
                        <div class="tank-type">
                            <div class="tank-icon">💧</div>
                            <span>Water Storage Tanks</span>
                        </div>
                        <div class="tank-type">
                            <div class="tank-icon">🧪</div>
                            <span>Chemical Tanks</span>
                        </div>
                        <div class="tank-type">
                            <div class="tank-icon">🌾</div>
                            <span>Fertilizer Vessels</span>
                        </div>
                        <div class="tank-type">
                            <div class="tank-icon">🥛</div>
                            <span>Milk Cooling Tanks</span>
                        </div>
                        <div class="tank-type">
                            <div class="tank-icon">⚙️</div>
                            <span>Processing Equipment</span>
                        </div>
                        <div class="tank-type">
                            <div class="tank-icon">🏭</div>
                            <span>Industrial Vessels</span>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Process Overview -->
            <div class="showcase-card process-card">
                <div class="card-inner">
                    <h3>Our Tank Insulation Process</h3>
                    <div class="process-steps">
                        <div class="process-step">
                            <div class="step-number">1</div>
                            <div class="step-content">
                                <h4>Assessment</h4>
                                <p>Tank evaluation & heat loss analysis</p>
                            </div>
                        </div>
                        <div class="process-step">
                            <div class="step-number">2</div>
                            <div class="step-content">
                                <h4>Preparation</h4>
                                <p>Surface cleaning & protection setup</p>
                            </div>
                        </div>
                        <div class="process-step">
                            <div class="step-number">3</div>
                            <div class="step-content">
                                <h4>Application</h4>
                                <p>Precision spray foam installation</p>
                            </div>
                        </div>
                        <div class="process-step">
                            <div class="step-number">4</div>
                            <div class="step-content">
                                <h4>Protection</h4>
                                <p>UV coating & weather barrier</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Benefits Timeline -->
        <div class="benefits-timeline">
            <h3>Tank Insulation Benefits Over Time</h3>
            <div class="timeline-track">
                <div class="timeline-item" data-time="Immediate">
                    <div class="timeline-marker"></div>
                    <div class="timeline-content">
                        <h4>Immediate Benefits</h4>
                        <ul>
                            <li>Temperature stabilization</li>
                            <li>Reduced evaporation</li>
                            <li>UV protection</li>
                        </ul>
                    </div>
                </div>
                <div class="timeline-item" data-time="3 Months">
                    <div class="timeline-marker"></div>
                    <div class="timeline-content">
                        <h4>Short-term Impact</h4>
                        <ul>
                            <li>Measurable water savings</li>
                            <li>Lower energy costs</li>
                            <li>Improved efficiency</li>
                        </ul>
                    </div>
                </div>
                <div class="timeline-item" data-time="1 Year">
                    <div class="timeline-marker"></div>
                    <div class="timeline-content">
                        <h4>Annual Results</h4>
                        <ul>
                            <li>Significant ROI achieved</li>
                            <li>Extended equipment life</li>
                            <li>Reduced maintenance</li>
                        </ul>
                    </div>
                </div>
                <div class="timeline-item" data-time="5+ Years">
                    <div class="timeline-marker"></div>
                    <div class="timeline-content">
                        <h4>Long-term Value</h4>
                        <ul>
                            <li>Continued savings</li>
                            <li>Asset protection</li>
                            <li>Sustainable operations</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Interactive Calculator -->
        <div class="savings-calculator">
            <h3>Estimate Your Water Savings</h3>
            <div class="calculator-content">
                <div class="calculator-inputs">
                    <div class="input-group">
                        <label for="tank-size">Tank Capacity (gallons)</label>
                        <input type="number" id="tank-size" value="10000" min="1000" max="100000" step="1000">
                    </div>
                    <div class="input-group">
                        <label for="water-cost">Water Cost (per 1000 gallons)</label>
                        <input type="number" id="water-cost" value="5" min="1" max="20" step="0.5">
                    </div>
                </div>
                <div class="calculator-results">
                    <div class="result-item">
                        <span class="result-label">Daily Water Saved</span>
                        <span class="result-value" id="daily-savings">400 gallons</span>
                    </div>
                    <div class="result-item">
                        <span class="result-label">Annual Cost Savings</span>
                        <span class="result-value" id="annual-savings">$730</span>
                    </div>
                    <div class="result-item">
                        <span class="result-label">5-Year Savings</span>
                        <span class="result-value" id="five-year-savings">$3,650</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
/* Agricultural Tank Showcase Styles */
.agricultural-tank-showcase {
    padding: var(--space-xxl, 6rem) 0;
    position: relative;
    background: linear-gradient(180deg, rgba(10, 10, 10, 0.95) 0%, rgba(10, 10, 10, 1) 100%);
}

.section-header {
    text-align: center;
    margin-bottom: var(--space-xl, 4rem);
}

.section-header h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    margin-bottom: var(--space-md, 2rem);
    background: linear-gradient(135deg, #00ffff 0%, #ff6b35 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.section-subtitle {
    font-size: clamp(1.125rem, 2vw, 1.375rem);
    color: rgba(224, 224, 224, 0.8);
    max-width: 800px;
    margin: 0 auto;
}

/* Showcase Grid */
.tank-showcase-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: var(--space-lg, 3rem);
    margin-bottom: var(--space-xxl, 6rem);
}

.showcase-card {
    background: linear-gradient(135deg, rgba(0, 255, 255, 0.05) 0%, rgba(0, 255, 255, 0.02) 100%);
    border: 1px solid rgba(0, 255, 255, 0.2);
    border-radius: 20px;
    padding: var(--space-lg, 3rem);
    position: relative;
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

.showcase-card::before {
    content: '';
    position: absolute;
    top: -100%;
    left: -100%;
    width: 300%;
    height: 300%;
    background: radial-gradient(circle, rgba(0, 255, 255, 0.1) 0%, transparent 70%);
    opacity: 0;
    transition: opacity 0.3s ease;
}

.showcase-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 20px 40px rgba(0, 255, 255, 0.3);
}

.showcase-card:hover::before {
    opacity: 1;
}

.card-inner {
    position: relative;
    z-index: 1;
}

.showcase-card h3 {
    color: #00ffff;
    margin-bottom: var(--space-md, 2rem);
    font-size: 1.5rem;
}

/* Stats Card */
.stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-md, 2rem);
    text-align: center;
}

.stat-item {
    padding: var(--space-sm, 1rem);
}

.stat-number {
    font-size: 2.5rem;
    font-weight: 700;
    color: #ff6b35;
    display: block;
    margin-bottom: 0.5rem;
    text-shadow: 0 0 20px rgba(255, 107, 53, 0.5);
}

.stat-label {
    font-size: 0.875rem;
    color: rgba(224, 224, 224, 0.8);
    text-transform: uppercase;
    letter-spacing: 0.05em;
}

/* Tank Types */
.tank-types-list {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-sm, 1rem);
}

.tank-type {
    display: flex;
    align-items: center;
    gap: var(--space-sm, 1rem);
    padding: var(--space-sm, 1rem);
    background: rgba(255, 255, 255, 0.03);
    border-radius: 10px;
    transition: all 0.3s ease;
}

.tank-type:hover {
    background: rgba(255, 107, 53, 0.1);
    transform: translateX(5px);
}

.tank-icon {
    font-size: 2rem;
    filter: drop-shadow(0 0 10px rgba(255, 107, 53, 0.5));
}

/* Process Steps */
.process-steps {
    display: flex;
    flex-direction: column;
    gap: var(--space-md, 2rem);
}

.process-step {
    display: flex;
    align-items: center;
    gap: var(--space-md, 2rem);
}

.step-number {
    width: 50px;
    height: 50px;
    background: linear-gradient(135deg, #ff6b35, #ff8855);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 1.25rem;
    flex-shrink: 0;
    box-shadow: 0 5px 15px rgba(255, 107, 53, 0.3);
}

.step-content h4 {
    color: #00ffff;
    margin-bottom: 0.25rem;
}

.step-content p {
    color: rgba(224, 224, 224, 0.8);
    font-size: 0.875rem;
}

/* Benefits Timeline */
.benefits-timeline {
    margin-bottom: var(--space-xxl, 6rem);
}

.benefits-timeline h3 {
    text-align: center;
    font-size: 2rem;
    margin-bottom: var(--space-xl, 4rem);
    color: #ffffff;
}

.timeline-track {
    position: relative;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-lg, 3rem);
}

.timeline-track::before {
    content: '';
    position: absolute;
    top: 30px;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(90deg, #00ffff, #ff6b35, #00ffff);
    z-index: 0;
}

.timeline-item {
    position: relative;
    text-align: center;
}

.timeline-item::before {
    content: attr(data-time);
    position: absolute;
    top: -40px;
    left: 50%;
    transform: translateX(-50%);
    color: #ff6b35;
    font-weight: 600;
    font-size: 0.875rem;
}

.timeline-marker {
    width: 60px;
    height: 60px;
    background: #0a0a0a;
    border: 3px solid #00ffff;
    border-radius: 50%;
    margin: 0 auto var(--space-md, 2rem);
    position: relative;
    z-index: 1;
    transition: all 0.3s ease;
}

.timeline-marker::after {
    content: '';
    position: absolute;
    inset: 10px;
    background: #00ffff;
    border-radius: 50%;
    animation: pulse-marker 2s ease-in-out infinite;
}

@keyframes pulse-marker {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(0.8); opacity: 0.5; }
}

.timeline-item:hover .timeline-marker {
    transform: scale(1.2);
    box-shadow: 0 0 30px rgba(0, 255, 255, 0.6);
}

.timeline-content {
    background: rgba(10, 10, 10, 0.8);
    border: 1px solid rgba(0, 255, 255, 0.2);
    border-radius: 15px;
    padding: var(--space-md, 2rem);
}

.timeline-content h4 {
    color: #00ffff;
    margin-bottom: var(--space-sm, 1rem);
}

.timeline-content ul {
    list-style: none;
    padding: 0;
    margin: 0;
}

.timeline-content li {
    padding: 0.25rem 0;
    color: rgba(224, 224, 224, 0.8);
    font-size: 0.875rem;
}

/* Savings Calculator */
.savings-calculator {
    background: linear-gradient(135deg, rgba(255, 107, 53, 0.1) 0%, rgba(0, 255, 255, 0.1) 100%);
    border: 2px solid transparent;
    border-image: linear-gradient(135deg, #ff6b35, #00ffff) 1;
    border-radius: 30px;
    padding: var(--space-xl, 4rem);
    text-align: center;
}

.savings-calculator h3 {
    font-size: 2rem;
    margin-bottom: var(--space-lg, 3rem);
    background: linear-gradient(135deg, #ff6b35, #00ffff);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.calculator-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-xl, 4rem);
    align-items: center;
}

.calculator-inputs {
    display: flex;
    flex-direction: column;
    gap: var(--space-md, 2rem);
}

.input-group {
    text-align: left;
}

.input-group label {
    display: block;
    margin-bottom: 0.5rem;
    color: rgba(224, 224, 224, 0.9);
    font-weight: 500;
}

.input-group input {
    width: 100%;
    padding: 1rem;
    background: rgba(10, 10, 10, 0.6);
    border: 1px solid rgba(0, 255, 255, 0.3);
    border-radius: 10px;
    color: #ffffff;
    font-size: 1.125rem;
    transition: all 0.3s ease;
}

.input-group input:focus {
    outline: none;
    border-color: #00ffff;
    box-shadow: 0 0 20px rgba(0, 255, 255, 0.3);
}

.calculator-results {
    display: flex;
    flex-direction: column;
    gap: var(--space-md, 2rem);
}

.result-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--space-sm, 1rem);
    background: rgba(10, 10, 10, 0.6);
    border-radius: 10px;
}

.result-label {
    color: rgba(224, 224, 224, 0.8);
}

.result-value {
    font-size: 1.5rem;
    font-weight: 700;
    color: #ff6b35;
    text-shadow: 0 0 20px rgba(255, 107, 53, 0.5);
}

/* Responsive */
@media (max-width: 968px) {
    .tank-showcase-grid {
        grid-template-columns: 1fr;
    }
    
    .timeline-track::before {
        display: none;
    }
    
    .calculator-content {
        grid-template-columns: 1fr;
    }
    
    .stats-grid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 768px) {
    .tank-types-list {
        grid-template-columns: 1fr;
    }
}
</style>

<script>
// Savings Calculator Logic
document.addEventListener('DOMContentLoaded', function() {
    const tankSizeInput = document.getElementById('tank-size');
    const waterCostInput = document.getElementById('water-cost');
    const dailySavingsEl = document.getElementById('daily-savings');
    const annualSavingsEl = document.getElementById('annual-savings');
    const fiveYearSavingsEl = document.getElementById('five-year-savings');
    
    function calculateSavings() {
        const tankSize = parseFloat(tankSizeInput.value);
        const waterCost = parseFloat(waterCostInput.value);
        
        // Estimate 4% daily evaporation without insulation, reduced to 0.8% with insulation
        const dailyEvaporationSaved = tankSize * 0.032; // 3.2% savings
        const annualGallonsSaved = dailyEvaporationSaved * 365;
        const annualCostSavings = (annualGallonsSaved / 1000) * waterCost;
        const fiveYearSavings = annualCostSavings * 5;
        
        // Update display
        dailySavingsEl.textContent = Math.round(dailyEvaporationSaved).toLocaleString() + ' gallons';
        annualSavingsEl.textContent = '$' + Math.round(annualCostSavings).toLocaleString();
        fiveYearSavingsEl.textContent = '$' + Math.round(fiveYearSavings).toLocaleString();
    }
    
    // Add event listeners
    tankSizeInput.addEventListener('input', calculateSavings);
    waterCostInput.addEventListener('input', calculateSavings);
    
    // Initial calculation
    calculateSavings();
    
    // Animate stats on scroll
    const observerOptions = {
        threshold: 0.5,
        rootMargin: '0px'
    };
    
    const statsObserver = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                const statNumbers = entry.target.querySelectorAll('.stat-number');
                statNumbers.forEach(stat => {
                    const finalValue = stat.getAttribute('data-value');
                    const isPercent = stat.textContent.includes('%');
                    const isDollar = stat.textContent.includes('$');
                    const isPlus = stat.textContent.includes('+');
                    
                    let current = 0;
                    const increment = finalValue / 50;
                    const timer = setInterval(() => {
                        current += increment;
                        if (current >= finalValue) {
                            current = finalValue;
                            clearInterval(timer);
                        }
                        
                        let display = Math.round(current);
                        if (isPercent) display += '%';
                        if (isDollar) display = '$' + display + 'K';
                        if (isPlus) display += '+';
                        
                        stat.textContent = display;
                    }, 30);
                });
                statsObserver.unobserve(entry.target);
            }
        });
    }, observerOptions);
    
    const statsCard = document.querySelector('.stats-card');
    if (statsCard) {
        statsObserver.observe(statsCard);
    }
});
</script>
