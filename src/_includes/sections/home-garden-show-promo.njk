<!-- Home & Garden Show 2025 Promotional Component -->
<section class="home-garden-show-promo">
  <div class="promo-glow"></div>
  
  <div class="container">
    <div class="promo-content">
      <!-- Header -->
      <div class="promo-header">
        <div class="event-badge">
          <span class="badge-icon">★</span>
          <span>EVENT ANNOUNCEMENT</span>
        </div>
        
        <h2 class="promo-title">
          Home & Garden Show
          <span class="year">2025</span>
        </h2>
        
        <div class="event-info">
          <span>July 11-13</span>
          <span class="separator">•</span>
          <span>Phoenix Convention Center</span>
          <span class="separator">•</span>
          <span class="booth">Booth #1247</span>
        </div>
      </div>
      
      <!-- Main Content -->
      <p class="promo-description">
        Join us for Arizona's premier home improvement event. Experience live spray foam 
        insulation demonstrations and meet our expert team.
      </p>
      
      <!-- Features -->
      <div class="feature-list">
        <div class="feature">
          <div class="feature-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="10"/>
              <polygon points="10 8 16 12 10 16 10 8"/>
            </svg>
          </div>
          <div>
            <h3>Live Demonstrations</h3>
            <p>See spray foam application in action</p>
          </div>
        </div>
        
        <div class="feature">
          <div class="feature-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
              <circle cx="12" cy="7" r="4"/>
            </svg>
          </div>
          <div>
            <h3>Expert Consultations</h3>
            <p>Free personalized assessments</p>
          </div>
        </div>
        
        <div class="feature">
          <div class="feature-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/>
            </svg>
          </div>
          <div>
            <h3>Exclusive Show Benefits</h3>
            <p>Special pricing for attendees only</p>
          </div>
        </div>
      </div>
      
      <!-- CTA -->
      <div class="cta-section">
        <a href="/events/home-garden-show-2025/" class="btn-primary">
          <span>Learn More About The Event</span>
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="5" y1="12" x2="19" y2="12"/>
            <polyline points="12 5 19 12 12 19"/>
          </svg>
        </a>
        <a href="/events/home-garden-show-2025/#lead-form" class="btn-ghost">
          Register for Updates
        </a>
      </div>
    </div>
  </div>
</section>

<style>
.home-garden-show-promo {
  position: relative;
  padding: calc(var(--spacing-xl) * 2.5) 0;
  background: linear-gradient(180deg, #0A0A0A 0%, #0f0f0f 100%);
  overflow: hidden;
}

/* Glow Effect */
.promo-glow {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 800px;
  height: 800px;
  background: radial-gradient(circle, rgba(255,107,0,0.03) 0%, transparent 50%);
  transform: translate(-50%, -50%);
  pointer-events: none;
}

.promo-content {
  max-width: 800px;
  margin: 0 auto;
  text-align: center;
  position: relative;
  z-index: 1;
}

/* Header */
.promo-header {
  margin-bottom: 3rem;
}

.event-badge {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.5rem 1.25rem;
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 50px;
  font-size: 0.75rem;
  font-weight: 600;
  letter-spacing: 0.1em;
  color: rgba(255,255,255,0.7);
  margin-bottom: 2rem;
}

.badge-icon {
  color: var(--color-primary);
  font-size: 1rem;
}

.promo-title {
  font-size: clamp(2.5rem, 6vw, 4rem);
  font-weight: 200;
  color: #ffffff;
  line-height: 1.1;
  margin-bottom: 1.5rem;
  letter-spacing: -0.02em;
}

.promo-title .year {
  display: inline-block;
  font-weight: 800;
  color: var(--color-primary);
  margin-left: 0.5rem;
}

.event-info {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.75rem;
  font-size: 1rem;
  color: rgba(255,255,255,0.6);
  flex-wrap: wrap;
}

.separator {
  color: rgba(255,255,255,0.3);
}

.booth {
  color: var(--color-primary);
  font-weight: 600;
}

/* Description */
.promo-description {
  font-size: 1.25rem;
  line-height: 1.7;
  color: rgba(255,255,255,0.8);
  margin-bottom: 3rem;
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
}

/* Features */
.feature-list {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
  margin-bottom: 3rem;
  text-align: left;
}

.feature {
  display: flex;
  gap: 1rem;
  padding: 1.5rem;
  background: rgba(255,255,255,0.02);
  border: 1px solid rgba(255,255,255,0.05);
  border-radius: 1rem;
  transition: all 0.3s ease;
}

.feature:hover {
  background: rgba(255,255,255,0.04);
  border-color: rgba(255,107,0,0.2);
  transform: translateY(-2px);
}

.feature-icon {
  width: 48px;
  height: 48px;
  background: rgba(255,107,0,0.1);
  border-radius: 0.75rem;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.feature-icon svg {
  width: 24px;
  height: 24px;
  stroke: var(--color-primary);
}

.feature h3 {
  font-size: 1.125rem;
  font-weight: 600;
  color: #ffffff;
  margin-bottom: 0.25rem;
}

.feature p {
  font-size: 0.875rem;
  color: rgba(255,255,255,0.6);
  line-height: 1.4;
}

/* CTA Section */
.cta-section {
  display: flex;
  gap: 1rem;
  justify-content: center;
  flex-wrap: wrap;
}

.btn-primary {
  display: inline-flex;
  align-items: center;
  gap: 0.75rem;
  padding: 1.25rem 2.5rem;
  background: var(--color-primary);
  color: white;
  text-decoration: none;
  border-radius: 0.5rem;
  font-weight: 600;
  font-size: 1.05rem;
  transition: all 0.3s ease;
}

.btn-primary:hover {
  background: #e55a00;
  transform: translateY(-2px);
  box-shadow: 0 10px 30px rgba(255,107,0,0.3);
}

.btn-primary svg {
  width: 20px;
  height: 20px;
  transition: transform 0.3s ease;
}

.btn-primary:hover svg {
  transform: translateX(4px);
}

.btn-ghost {
  display: inline-flex;
  align-items: center;
  padding: 1.25rem 2.5rem;
  background: transparent;
  color: rgba(255,255,255,0.8);
  text-decoration: none;
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 0.5rem;
  font-weight: 500;
  font-size: 1.05rem;
  transition: all 0.3s ease;
}

.btn-ghost:hover {
  color: white;
  border-color: rgba(255,255,255,0.3);
  background: rgba(255,255,255,0.05);
}

/* Responsive */
@media (max-width: 768px) {
  .home-garden-show-promo {
    padding: var(--spacing-xl) 0;
  }
  
  .promo-title {
    font-size: 2rem;
  }
  
  .event-info {
    font-size: 0.875rem;
  }
  
  .promo-description {
    font-size: 1.125rem;
  }
  
  .feature-list {
    grid-template-columns: 1fr;
    gap: 1rem;
  }
  
  .cta-section {
    flex-direction: column;
  }
  
  .btn-primary,
  .btn-ghost {
    width: 100%;
    justify-content: center;
    padding: 1rem 2rem;
  }
}
</style>