<!-- Home & Garden Show 2025 Promotional Component -->
<section class="home-garden-show-promo">
  <div class="container">
    <div class="show-promo-content">
      <!-- Event Badge -->
      <div class="event-badge">
        <svg class="badge-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
          <line x1="16" y1="2" x2="16" y2="6"/>
          <line x1="8" y1="2" x2="8" y2="6"/>
          <line x1="3" y1="10" x2="21" y2="10"/>
        </svg>
        <div class="badge-content">
          <span class="badge-label">Limited Time Event</span>
          <span class="badge-dates">July 11-13, 2025</span>
        </div>
      </div>
      
      <h2 class="show-title">
        Meet Us at Phoenix's Premier<br>
        <span class="highlight">Home & Garden Show</span>
      </h2>
      
      <p class="show-subtitle">
        Phoenix Convention Center • Booth #1247
      </p>
      
      <p class="show-description">
        Join Insulation Contractors of Arizona for exclusive show pricing, live spray foam demonstrations, 
        and expert consultations. Don't miss this opportunity to save big on your home's energy efficiency!
      </p>
      
      <!-- Features Grid -->
      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon-wrapper">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 2L2 7l10 5 10-5-10-5z"/>
              <path d="M2 17l10 5 10-5"/>
              <path d="M2 12l10 5 10-5"/>
            </svg>
          </div>
          <h3>Live Demonstrations</h3>
          <p>Watch spray foam insulation applied in real-time by our certified professionals</p>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon-wrapper">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/>
            </svg>
          </div>
          <h3>Free Consultations</h3>
          <p>Get personalized advice for your home's specific insulation needs</p>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon-wrapper">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="2" y="4" width="20" height="16" rx="2"/>
              <path d="M7 15h0M2 9h20"/>
            </svg>
          </div>
          <h3>Show-Only Pricing</h3>
          <p>Exclusive discounts available only during the event weekend</p>
        </div>
      </div>
      
      <!-- Special Offer -->
      <div class="offer-banner">
        <div class="offer-content">
          <div class="offer-text">
            <span class="offer-label">EXCLUSIVE SHOW SPECIAL</span>
            <span class="offer-amount">Save up to $500</span>
            <span class="offer-details">on whole-home insulation projects booked at the show</span>
          </div>
          <div class="offer-badge">
            <span class="badge-text">Limited<br>Time</span>
          </div>
        </div>
      </div>
      
      <!-- CTA Section -->
      <div class="cta-section">
        <a href="/events/home-garden-show-2025/" class="btn-primary">
          View Event Details
          <svg class="btn-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="5" y1="12" x2="19" y2="12"/>
            <polyline points="12 5 19 12 12 19"/>
          </svg>
        </a>
        <a href="/events/home-garden-show-2025/#lead-form" class="btn-secondary">
          Pre-Register for VIP Offers
        </a>
      </div>
    </div>
  </div>
</section>

<style>
.home-garden-show-promo {
  padding: calc(var(--spacing-xl) * 2) 0;
  background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
  position: relative;
  overflow: hidden;
}

.home-garden-show-promo::before {
  content: '';
  position: absolute;
  top: -200px;
  right: -200px;
  width: 400px;
  height: 400px;
  background: radial-gradient(circle, rgba(255,107,0,0.08) 0%, transparent 60%);
  pointer-events: none;
}

.show-promo-content {
  max-width: 900px;
  margin: 0 auto;
  text-align: center;
}

/* Event Badge */
.event-badge {
  display: inline-flex;
  align-items: center;
  gap: 1rem;
  padding: 0.875rem 1.5rem;
  background: var(--color-primary);
  color: white;
  border-radius: 100px;
  margin-bottom: var(--spacing-lg);
  box-shadow: 0 4px 20px rgba(255,107,0,0.25);
}

.badge-icon {
  width: 24px;
  height: 24px;
  flex-shrink: 0;
}

.badge-content {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 0.125rem;
}

.badge-label {
  font-size: 0.75rem;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  opacity: 0.9;
}

.badge-dates {
  font-size: 1rem;
  font-weight: 800;
}

/* Title Section */
.show-title {
  font-size: clamp(2rem, 5vw, 3.5rem);
  font-weight: 300;
  color: #1a1a1a;
  line-height: 1.2;
  margin-bottom: 1rem;
  letter-spacing: -0.02em;
}

.show-title .highlight {
  font-weight: 800;
  color: var(--color-primary);
  display: block;
  margin-top: 0.25rem;
}

.show-subtitle {
  font-size: 1.25rem;
  color: #666;
  margin-bottom: 1.5rem;
  font-weight: 500;
}

.show-description {
  font-size: 1.125rem;
  color: #555;
  line-height: 1.7;
  max-width: 700px;
  margin: 0 auto var(--spacing-xl);
}

/* Features Grid */
.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
  margin-bottom: var(--spacing-xl);
}

.feature-card {
  background: white;
  padding: 2rem;
  border-radius: 1rem;
  box-shadow: 0 2px 20px rgba(0,0,0,0.06);
  transition: all 0.3s ease;
  border: 1px solid #f0f0f0;
}

.feature-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 8px 30px rgba(0,0,0,0.1);
  border-color: rgba(255,107,0,0.2);
}

.feature-icon-wrapper {
  width: 60px;
  height: 60px;
  background: linear-gradient(135deg, rgba(255,107,0,0.1) 0%, rgba(255,107,0,0.05) 100%);
  border-radius: 1rem;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 1rem;
}

.feature-icon-wrapper svg {
  width: 28px;
  height: 28px;
  color: var(--color-primary);
}

.feature-card h3 {
  font-size: 1.25rem;
  font-weight: 700;
  color: #1a1a1a;
  margin-bottom: 0.5rem;
}

.feature-card p {
  font-size: 0.9rem;
  color: #666;
  line-height: 1.6;
}

/* Offer Banner */
.offer-banner {
  background: linear-gradient(135deg, var(--color-primary) 0%, #ff8c42 100%);
  border-radius: 1.5rem;
  padding: 2.5rem;
  margin-bottom: var(--spacing-xl);
  box-shadow: 0 10px 40px rgba(255,107,0,0.3);
  position: relative;
  overflow: hidden;
}

.offer-banner::before {
  content: '';
  position: absolute;
  top: -50%;
  right: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 50%);
  animation: pulse 4s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% { transform: scale(1); opacity: 0.5; }
  50% { transform: scale(1.1); opacity: 0.8; }
}

.offer-content {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 2rem;
}

.offer-text {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  text-align: left;
}

.offer-label {
  font-size: 0.875rem;
  font-weight: 700;
  color: rgba(255,255,255,0.9);
  text-transform: uppercase;
  letter-spacing: 0.15em;
  margin-bottom: 0.5rem;
}

.offer-amount {
  font-size: 2.5rem;
  font-weight: 900;
  color: white;
  line-height: 1;
  margin-bottom: 0.5rem;
}

.offer-details {
  font-size: 1rem;
  color: rgba(255,255,255,0.9);
}

.offer-badge {
  width: 80px;
  height: 80px;
  background: rgba(255,255,255,0.2);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  backdrop-filter: blur(10px);
  border: 2px solid rgba(255,255,255,0.3);
}

.badge-text {
  font-size: 0.875rem;
  font-weight: 800;
  color: white;
  text-align: center;
  line-height: 1.2;
}

/* CTA Section */
.cta-section {
  display: flex;
  gap: 1rem;
  justify-content: center;
  flex-wrap: wrap;
}

.btn-primary,
.btn-secondary {
  display: inline-flex;
  align-items: center;
  gap: 0.75rem;
  padding: 1.125rem 2rem;
  border-radius: 0.5rem;
  font-weight: 700;
  text-decoration: none;
  transition: all 0.3s ease;
  font-size: 1rem;
}

.btn-primary {
  background: #1a1a1a;
  color: white;
  border: 2px solid #1a1a1a;
}

.btn-primary:hover {
  background: #333;
  border-color: #333;
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(0,0,0,0.2);
}

.btn-secondary {
  background: transparent;
  color: var(--color-primary);
  border: 2px solid var(--color-primary);
}

.btn-secondary:hover {
  background: var(--color-primary);
  color: white;
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(255,107,0,0.25);
}

.btn-arrow {
  width: 20px;
  height: 20px;
  transition: transform 0.3s ease;
}

.btn-primary:hover .btn-arrow {
  transform: translateX(4px);
}

/* Responsive */
@media (max-width: 768px) {
  .home-garden-show-promo {
    padding: var(--spacing-xl) 0;
  }
  
  .features-grid {
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }
  
  .offer-content {
    flex-direction: column;
    text-align: center;
  }
  
  .offer-text {
    align-items: center;
  }
  
  .offer-amount {
    font-size: 2rem;
  }
  
  .cta-section {
    flex-direction: column;
  }
  
  .btn-primary,
  .btn-secondary {
    width: 100%;
    justify-content: center;
  }
}

@media (max-width: 480px) {
  .show-title {
    font-size: 1.75rem;
  }
  
  .show-subtitle {
    font-size: 1rem;
  }
  
  .event-badge {
    padding: 0.75rem 1.25rem;
  }
  
  .badge-dates {
    font-size: 0.875rem;
  }
}
</style>