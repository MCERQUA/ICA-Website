<!-- Home & Garden Show 2025 Promotional Component -->
<section class="home-garden-show-promo">
  <div class="promo-background">
    <div class="animated-bg"></div>
  </div>
  
  <div class="container">
    <div class="promo-content">
      <div class="event-announcement">
        <div class="pulse-dot"></div>
        <span>LIMITED TIME EVENT</span>
      </div>
      
      <h2 class="promo-title">
        <span class="title-line-1">Phoenix Home &</span>
        <span class="title-line-2">Garden Show</span>
        <span class="title-line-3">2025</span>
      </h2>
      
      <div class="event-details">
        <div class="detail-item">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
            <line x1="16" y1="2" x2="16" y2="6"/>
            <line x1="8" y1="2" x2="8" y2="6"/>
            <line x1="3" y1="10" x2="21" y2="10"/>
          </svg>
          <span>July 11-13</span>
        </div>
        <div class="detail-item">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/>
            <circle cx="12" cy="10" r="3"/>
          </svg>
          <span>Phoenix Convention Center</span>
        </div>
        <div class="detail-item highlight">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M3 3h18v18H3zM12 8v8m-4-4h8"/>
          </svg>
          <span>Booth #1247</span>
        </div>
      </div>
      
      <p class="promo-description">
        Experience the future of home insulation with live spray foam demonstrations 
        and exclusive show-only pricing that will transform your home's energy efficiency.
      </p>
      
      <div class="cta-group">
        <a href="/events/home-garden-show-2025/" class="cta-primary enhanced">
          <span class="cta-text">Explore Event Details</span>
          <div class="cta-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <line x1="5" y1="12" x2="19" y2="12"/>
              <polyline points="12 5 19 12 12 19"/>
            </svg>
          </div>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
.home-garden-show-promo {
  position: relative;
  padding: calc(var(--spacing-xl) * 1.5) 0;
  background: #0A0A0A;
  overflow: hidden;
}

/* Animated Background - REDUCED OPACITY */
.promo-background {
  position: absolute;
  inset: 0;
  pointer-events: none;
  opacity: 0.06; /* Reduced from 0.15 */
}

.animated-bg {
  position: absolute;
  inset: -50%;
  background: 
    radial-gradient(circle at 20% 50%, rgba(255,107,0,0.08) 0%, transparent 50%),
    radial-gradient(circle at 80% 50%, rgba(255,107,0,0.05) 0%, transparent 50%);
  animation: rotate-bg 30s linear infinite;
}

@keyframes rotate-bg {
  from { transform: rotate(0deg) scale(1.5); }
  to { transform: rotate(360deg) scale(1.5); }
}

/* Content Section */
.promo-content {
  max-width: 700px;
  margin: 0 auto;
  text-align: center;
  color: white;
  position: relative;
  z-index: 1;
}

.event-announcement {
  display: inline-flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0.625rem 1.5rem;
  background: linear-gradient(135deg, #FFD700 0%, #FFA500 100%);
  border-radius: 100px;
  margin-bottom: 2rem;
  font-size: 0.75rem;
  font-weight: 800;
  letter-spacing: 0.15em;
  color: #000000;
  box-shadow: 0 4px 15px rgba(255,215,0,0.4);
  transition: all 0.3s ease;
}

.event-announcement:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(255,215,0,0.5);
}

.pulse-dot {
  width: 8px;
  height: 8px;
  background: #000000;
  border-radius: 50%;
  position: relative;
}

.pulse-dot::before {
  content: '';
  position: absolute;
  inset: -4px;
  background: rgba(0,0,0,0.5);
  border-radius: 50%;
  opacity: 0;
  animation: pulse-ring 2s ease-out infinite;
}

@keyframes pulse-ring {
  0% { transform: scale(0.5); opacity: 0.8; }
  100% { transform: scale(1.5); opacity: 0; }
}

.promo-title {
  font-size: clamp(2.5rem, 6vw, 4.5rem);
  line-height: 0.9;
  margin-bottom: 2rem;
  font-weight: 900;
  position: relative;
}

.title-line-1 {
  display: block;
  font-size: 0.5em;
  font-weight: 300;
  letter-spacing: 0.02em;
  opacity: 0.8;
  text-transform: uppercase;
}

.title-line-2 {
  display: block;
  color: #ffffff;
  margin: 0.1em 0;
  position: relative;
  z-index: 2;
}

.title-line-3 {
  display: block;
  font-size: 1.2em;
  color: var(--color-primary);
  position: relative;
  z-index: 2;
}

/* Reduced background effect opacity on title */
.title-line-2::after {
  content: 'Garden Show';
  position: absolute;
  left: 0;
  top: 0;
  background: linear-gradient(135deg, var(--color-primary) 0%, #ff8c42 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  opacity: 0.03; /* Reduced from 0.08 */
  z-index: -1;
  filter: blur(20px);
  transform: scale(1.1);
}

.event-details {
  display: flex;
  gap: 2rem;
  margin-bottom: 2rem;
  flex-wrap: wrap;
  justify-content: center;
}

.detail-item {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  color: rgba(255,255,255,0.7);
  font-size: 0.95rem;
  transition: color 0.3s ease;
}

.detail-item:hover {
  color: white;
}

.detail-item svg {
  width: 20px;
  height: 20px;
  stroke: currentColor;
}

.detail-item.highlight {
  color: var(--color-primary);
  font-weight: 700;
}

.promo-description {
  font-size: 1.125rem;
  line-height: 1.7;
  color: rgba(255,255,255,0.8);
  margin-bottom: 2.5rem;
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
}

/* Enhanced CTA Button */
.cta-group {
  display: flex;
  justify-content: center;
}

.cta-primary {
  display: inline-flex;
  align-items: center;
  gap: 1rem;
  padding: 1.5rem 3rem; /* Increased padding */
  background: linear-gradient(135deg, var(--color-primary) 0%, #ff8c42 100%);
  color: white;
  text-decoration: none;
  border-radius: 100px;
  font-weight: 800; /* Increased font weight */
  font-size: 1.125rem; /* Larger font size */
  letter-spacing: 0.02em;
  transition: all 0.4s ease;
  position: relative;
  overflow: hidden;
  box-shadow: 
    0 8px 25px rgba(255,107,0,0.4),
    0 0 0 1px rgba(255,255,255,0.1) inset;
}

/* Enhanced glow effect */
.cta-primary::before {
  content: '';
  position: absolute;
  inset: -2px;
  background: linear-gradient(45deg, transparent, rgba(255,255,255,0.4), transparent);
  border-radius: 100px;
  opacity: 0;
  transition: opacity 0.3s ease;
  z-index: -1;
}

.cta-primary::after {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(45deg, transparent, rgba(255,255,255,0.2), transparent);
  transform: translateX(-100%);
  transition: transform 0.6s ease;
}

.cta-primary:hover {
  transform: translateY(-3px) scale(1.02);
  box-shadow: 
    0 15px 40px rgba(255,107,0,0.5),
    0 0 30px rgba(255,107,0,0.3),
    0 0 0 1px rgba(255,255,255,0.2) inset;
}

.cta-primary:hover::before {
  opacity: 1;
}

.cta-primary:hover::after {
  transform: translateX(100%);
}

/* Pulse animation for enhanced attention */
.cta-primary.enhanced {
  animation: cta-pulse 3s ease-in-out infinite;
}

@keyframes cta-pulse {
  0%, 100% { 
    box-shadow: 
      0 8px 25px rgba(255,107,0,0.4),
      0 0 0 1px rgba(255,255,255,0.1) inset;
  }
  50% { 
    box-shadow: 
      0 8px 25px rgba(255,107,0,0.4),
      0 0 0 1px rgba(255,255,255,0.1) inset,
      0 0 0 4px rgba(255,107,0,0.2);
  }
}

.cta-icon {
  width: 24px;
  height: 24px;
  transition: transform 0.3s ease;
}

.cta-primary:hover .cta-icon {
  transform: translateX(6px);
}

/* Responsive */
@media (max-width: 640px) {
  .home-garden-show-promo {
    padding: var(--spacing-xl) 0;
  }
  
  .promo-title {
    font-size: 2rem;
  }
  
  .event-details {
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }
  
  .cta-primary {
    padding: 1.25rem 2.5rem;
    font-size: 1rem;
  }
}
</style>