---
layout: base.njk
---

<article class="blog-post">
    <div class="container">
        <header class="post-header">
            <div class="post-meta">
                <time datetime="{{ date | htmlDateString }}">{{ date | postDate }}</time>
                {% if author %}
                <span class="author">by {{ author }}</span>
                {% endif %}
                {% if tags %}
                <div class="tags">
                    {% for tag in tags %}
                    <span class="tag">{{ tag }}</span>
                    {% endfor %}
                </div>
                {% endif %}
            </div>
        </header>
        
        <div class="post-content">
            {{ content | safe }}
        </div>
        
        <footer class="post-footer">
            <a href="/blog/" class="back-link">← Browse Arizona Insulation Articles</a>
        </footer>
    </div>
</article>