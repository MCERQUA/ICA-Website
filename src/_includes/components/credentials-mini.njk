{# Credentials Mini Component - Sidebar credentials display #}
{# 
  Usage: {% include "components/credentials-mini.njk" %}
#}

<div class="credentials-mini">
  <h3>Why Choose ICA?</h3>
  
  <div class="credential-item">
    <div class="credential-icon">✓</div>
    <div class="credential-text">
      <strong>20+ Years Experience</strong>
      <span>Arizona Insulation Experts</span>
    </div>
  </div>
  
  <div class="credential-item">
    <div class="credential-icon">✓</div>
    <div class="credential-text">
      <strong>Licensed & Insured</strong>
      <span>ROC# 334041</span>
    </div>
  </div>
  
  <div class="credential-item">
    <div class="credential-icon">✓</div>
    <div class="credential-text">
      <strong>BBB A+ Rating</strong>
      <span>Accredited Since 2022</span>
    </div>
  </div>
  
  <div class="credential-item">
    <div class="credential-icon">✓</div>
    <div class="credential-text">
      <strong>Icynene Certified</strong>
      <span>Premium Spray Foam Specialist</span>
    </div>
  </div>
  
  <div class="credential-item">
    <div class="credential-icon">✓</div>
    <div class="credential-text">
      <strong>SPFA Member</strong>
      <span>Industry Leadership</span>
    </div>
  </div>
  
  <div class="credential-item">
    <div class="credential-icon">✓</div>
    <div class="credential-text">
      <strong>Safety Certified</strong>
      <span>Hardison Downey Academy</span>
    </div>
  </div>
  
  <div class="cta-mini">
    <a href="/about/" class="learn-more">Learn More About Us</a>
  </div>
</div>

<style>
.credentials-mini {
  background: var(--surface-dark);
  border: 1px solid var(--border-color);
  border-radius: 8px;
  padding: 2rem;
  margin-top: 2rem;
}

.credentials-mini h3 {
  font-size: 1.5rem;
  color: var(--primary);
  margin-bottom: 1.5rem;
  text-transform: uppercase;
  letter-spacing: 1px;
  background: var(--text-gradient);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.credential-item {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  margin-bottom: 1.25rem;
  padding-bottom: 1.25rem;
  border-bottom: 1px solid rgba(0, 255, 255, 0.1);
}

.credential-item:last-of-type {
  border-bottom: none;
  margin-bottom: 1.5rem;
}

.credential-icon {
  width: 30px;
  height: 30px;
  background: var(--glow-gradient);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--surface-dark);
  font-weight: bold;
  font-size: 1.2rem;
  flex-shrink: 0;
  position: relative;
}

.credential-icon::after {
  content: '';
  position: absolute;
  top: -2px;
  left: -2px;
  right: -2px;
  bottom: -2px;
  background: var(--glow-gradient);
  border-radius: 50%;
  opacity: 0.3;
  z-index: -1;
  animation: pulse 2s ease-in-out infinite;
}

.credential-text {
  display: flex;
  flex-direction: column;
}

.credential-text strong {
  color: var(--primary);
  font-size: 0.95rem;
  margin-bottom: 0.25rem;
}

.credential-text span {
  color: var(--text-light);
  font-size: 0.85rem;
}

.cta-mini {
  text-align: center;
}

.learn-more {
  display: inline-block;
  padding: 0.75rem 1.5rem;
  background: transparent;
  border: 1px solid var(--glow-primary);
  color: var(--glow-primary);
  text-decoration: none;
  font-weight: bold;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  font-size: 0.9rem;
  border-radius: 4px;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.learn-more::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: var(--glow-gradient);
  transition: left 0.3s ease;
  z-index: -1;
}

.learn-more:hover {
  color: var(--surface-dark);
  border-color: transparent;
}

.learn-more:hover::before {
  left: 0;
}

@media (max-width: 768px) {
  .credentials-mini {
    margin-bottom: 2rem;
  }
}
</style>
